package itish.Interfaces.nested;

public class A {
    // nested interface can be decalread as public,private,protected
    // but top level interface has to be decalared as public and default ones only.
    public interface nestedInterface{
        boolean isOdd(int num);
    }

}
